<h2 align="center">Title: <%= @article.title %></h2>
<div class="well col-xs-10 col-xs-offset-1">
  <h4 class="center description"><strong>Description:</strong></h4>
  <hr>
  <%= simple_format(@article.description) %></p>
  <div class="article-actions">
    <%= link_to "Edit this article", edit_article_path(@article), class: "btn btn-xs btn-primary" %>
    <%= link_to "Delete this article", article_path(@article), method: :delete, data: {confirm: "Are you sutre you want to delete this article?"},class: "btn btn-xs btn-danger" %>
    <%= link_to "View all articless", articles_path(@article), class: "btn btn-xs btn-success" %>
  </div>
</div>
